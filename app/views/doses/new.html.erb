<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: @ingredients, prompt: "Choose an ingredient" %>
  <%= f.submit "Add a dose", class: "btn btn-primary" %>
<% end %>
